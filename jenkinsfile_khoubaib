pipeline{

      agent any
       
        tools {
               jdk 'JAVA_HOME'
               maven 'M2_HOME'
               }

        
stages{
	
	 
        stage('Getting project from Github') {
            steps {
	         script {
                       git branch : 'stock' ,
                       url : 'https://github.com/ahmedaziz-code/app-achat.git';
                  }
			 }
			                     }
	
	stage ('Unit Test') {
            steps{
	          script {
                        sh 'mvn  test'
                 }
		         }
                            }
       stage ('SonarQube analysis') {
            steps{
                sh 'mvn sonar:sonar'
		-D sonar.host.url=http://http://192.168.33.10:9000
		-D sonar.login=4465162476cbdfaa04558d7c71d68f8f6834ebfe
		-D sonar.login=admin
		-D sonar.password=admin
            }
        }
                                       
	   stage("Maven Build") {
                steps {
                    script {
                         sh "mvn package -DskipTests=true"
                           }
                      }
                               }
        }
}
